## GraphQL + PostgreSQL

#### includes

 - JWT auth throw `express-jwt`
 - `login` resolver
 - `get users` resolver
 
 
#### setup

 - copy `.env.example` to `.env` && set up env variables
 - `npm install`
 - `npm run m`
 - `npm run dev`
